{
  "name": "tauri-dev-mcp-test",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npm run lint && jest",
    "test:integration": "npm run lint && jest __tests__/integration.test.ts",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.29",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "typescript": "^5.8.3"
  }
}
